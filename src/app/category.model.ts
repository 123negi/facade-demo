export interface Category {
  length: number;
  limit:number;
  products:any[];
  skip:number;
  total:number;
  active:boolean;
    // Add additional properties as needed
  }
  
  export interface product {
    length: number;
    limit:number;
    products:any[];
    skip:number;
    total:number;
    active:boolean;
      // Add additional properties as needed
    }